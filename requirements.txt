fastapi==0.115.6
uvicorn[standard]==0.34.0
yfinance==1.5.1
curl_cffi==0.15.0
pandas==2.2.3
numpy==2.2.1
apscheduler==3.10.4
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
httpx==0.28.1
# cTrader Open API message classes are vendored (app/trading/ctrader_proto/), not
# pip-installed, to avoid the official ctrader-open-api package's Twisted +
# protobuf==3.20.1 pins (no Python 3.12 wheel). Only the protobuf runtime itself
# is a real dependency, pinned to match the version the vendored *_pb2.py files
# were generated with.
protobuf==7.35.1
